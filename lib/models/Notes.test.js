const Notes = require('./Notes'); 

describe('Notes', () => {
  it('needs to have a text entry', () => {
    const note = new Notes({
      categoy: 'Science', 
      entry: expect.any(Array)
    }); 
    expect(note.)
      
  });

}); 
